To test the AI, paste the "test_data.csv" containing testing datas to this folder.
Run the "evaluation.py" file. (If not trained, this project got trained before)
Run the "self_learning" file. (If not trained, this project got trained before)
After that, run the "inserting_data.py", get the answer in the "test_data_after_training.txt" file